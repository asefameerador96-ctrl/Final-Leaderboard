# Frontend: Chatbot backend API
VITE_CHATBOT_BACKEND_URL=http://localhost:3001

# Backend server (in server/.env):
# PORT=3001
# N8N_WEBHOOK_URL=https://your-n8n-instance.example.com/webhook/chat

# Environment Variables Example
# Copy this to .env.local and fill in actual values if needed in production

# API Configuration (for future backend integration)
# VITE_API_URL=https://api.example.com

# Feature Flags
# VITE_ENABLE_ANALYTICS=true
# VITE_ENABLE_LOGGING=false

# Note: Currently, this app uses browser localStorage for all data
# In production, you should:
# 1. Move authentication to backend with secure token storage
# 2. Store images on a CDN or cloud storage
# 3. Use a database for TSO data instead of localStorage
